# version: "3.9"

services:
  flask:
    build: ./flask_server
    container_name: flask_server
    ports:
      - "5000:5000"
    networks:
      - ai_net
    restart: always

networks:
  ai_net:
    driver: bridge